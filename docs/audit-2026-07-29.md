# DriverPit — Codebase Audit #2 (read-only)

> Point-in-time audit taken **2026-07-29** against the **working tree**, not a commit. `HEAD` is
> still `5b1a4aa` — the exact commit [audit-2026-07-27.md](./audit-2026-07-27.md) was taken
> against — with 49 modified/deleted files and 14 untracked paths on top of it. See **§0.3**; that
> gap is itself a finding.
>
> This is a **successor**, not a replacement. Everything the previous audit closed has been
> re-verified as closed against the current source and is deliberately **not repeated here**. What
> follows is three lists: what is still open, what is newly found, and one correction to a finding
> that was partly wrong when written.

## Method, and what this audit is and isn't

I read `audit-2026-07-27.md` end to end and checked every finding — including the ones marked ✅ —
against the files as they stand now. Where a resolution says "still outstanding", I confirmed the
outstanding half is genuinely still outstanding rather than taking the note's word for it. Where a
resolution claims something is fixed, I confirmed the fix is in the tree.

**What I could verify directly:** all source, SQL, migration and test files; `npx tsc --noEmit`
(clean); `npx vitest run` (**167 passed, 113 skipped across 26 files** — the 12 skipped files are
the `RUN_DB_INTEGRATION_TESTS=1` tier); the drizzle journal against the migration directory (46
entries, 46 files, no drift in either direction).

**What I could not verify:** anything about the **live database**. The DB integration tier needs a
reachable Supabase project, and CLAUDE.md records that a full run burns Supabase's per-IP anonymous
sign-in quota for ~an hour. So every claim below that a grant, constraint or RPC body is live in
Postgres is **inherited from the previous audit's verification**, not re-measured. `next build` was
also not run — CLAUDE.md records that it stalls before compiling on this machine, which is a
standing problem noted under §2.6.

**No code was changed.**

---

## Scoreboard

| Original audit | Count | Notes |
|---|---|---|
| Closed and verified closed | 17 | Including both CRITICALs, all three Server-Action forgery paths, the seed, win-by-identity |
| Partly closed — a named half still open | 5 | §2.5, §2.6, §3.0, §3.1, §3.6 |
| Open, unchanged | 16 | Led by §3.4 (realtime), §5.2 (unpinned release) |
| **New this audit** | **5** | §0.1–§0.5 |
| **Corrections to the previous audit** | **1** | §0.6 |

The quality of the fix work is high and worth saying plainly: every resolution I checked did what it
said, several found and closed something the finding hadn't seen, and the ones that were *scoped
out* said so instead of quietly shrinking. Two of the five new findings below (§0.1, §0.2) are
consequences of that work rather than of the original code — which is normal, and is exactly why a
second pass is worth doing.

---

## 0. New findings

### 0.1 A rematch runs with **no liveness heartbeat at all** — HIGH — reopens §3.3

This is the most serious thing in this audit. Drizzle/0040's server-verified absence check — the
whole of §3.3's fix, the thing that stops "forfeit your opponent on demand" — is **inert for the
entire duration of every rematch.**

The heartbeat lives in `DuelRoot`, which was the right call
([DuelRoot.tsx:140-162](../components/duel/DuelRoot.tsx#L140-L162)):

```ts
const heartbeatMatchId = phase === "landing" || phase === "searching" ? null : (match?.matchId ?? null);
useEffect(() => {
  if (heartbeatMatchId === null) return;
  ...
  const beat = async () => {
    const result = await matchHeartbeat(heartbeatMatchId);
    if (!cancelled && result === "terminal") stop();
  };
  ...
}, [heartbeatMatchId]);
```

`match` is `DuelRoot`'s own state. A rematch never touches it. `DuelMatch` receives `match` as a
prop and hands it to `useDuelLifecycle`, which copies it into its **own** state and mutates the id
on rematch ([useDuelLifecycle.ts:481-498](../components/duel/useDuelLifecycle.ts#L481-L498)):

```ts
function transitionToRematch(newMatchId: number) {
  ...
  setActiveMatch((prev) => ({ ...prev, matchId: newMatchId }));
}
```

There is **no callback up to `DuelRoot`** — I grepped every prop `DuelMatch` accepts
([DuelMatch.tsx:23-53](../components/duel/DuelMatch.tsx#L23-L53)); there is no `onMatchChange`.
So `DuelRoot.match.matchId` stays pinned to the *old* match for the whole rematch, and three things
follow:

1. **`heartbeatMatchId` still names the old match.** `duel_heartbeat` returns `false` for a match
   whose `status` is `finished`/`abandoned`
   ([0040:64-72](../drizzle/0040_duel_match_liveness.sql#L64-L72)), which `matchHeartbeat` maps to
   `"terminal"` ([matchHeartbeat.ts:31](../lib/duel/matchHeartbeat.ts#L31)) — so the interval
   **stops itself the moment the first match finishes**, i.e. on the results screen, before the
   rematch even exists. `heartbeatMatchId` never changes afterwards, so the effect never re-arms.
   The new match gets **zero** heartbeats from either client.
2. **`duel_matches.last_seen_a/b` on the rematch row therefore never move.** They are
   `NOT NULL DEFAULT now()` ([0040:35-38](../drizzle/0040_duel_match_liveness.sql#L35-L38)), stamped
   when `requestRematch` inserts the row
   ([actions.ts:494-497](../lib/duel/actions.ts#L494-L497)). `DISCONNECT_GRACE_MS` is **10 000 ms**.
   Staging alone is `MATCH_FOUND_HOLD_MS` 2 500 + `COUNTDOWN_MS` 3 900, so **both players are
   "stale" roughly four seconds into round 1** and stay stale for the remaining ~3 minutes 55.
   `forfeitMatch(newMatchId, opponentId)` from a devtools console then passes
   [actions.ts:194-199](../lib/duel/actions.ts#L194-L199) against a fully present opponent: status
   `abandoned`, `winner_id = attacker`, real Elo written to the column the leaderboard sorts on.
   That is §3.3's exploit verbatim, and rematch is a **primary CTA on the results screen**, so it is
   not an obscure path.
3. **`setLiveMatchId` publishes the old id too**
   ([DuelRoot.tsx:123-127](../components/duel/DuelRoot.tsx#L123-L127)). Signing out mid-rematch
   calls `forfeitMatch(oldMatchId)`, which `duel_forfeit` no-ops on an already-terminal match — so
   the opponent is stranded for the grace window instead of getting the immediate clean win
   CLAUDE.md promises under "Signing out mid-match forfeits it". The sign-out confirmation still
   fires (`matchLive` is truthy), so nothing looks wrong.

The honest-path damage is the mirror image: `declareOpponentForfeit`'s retry loop
([useDuelLifecycle.ts:675-681](../components/duel/useDuelLifecycle.ts#L675-L681)) now *succeeds
first try* in a rematch, because the server-side second opinion §3.3 added has nothing behind it.
The four-layer story reverts to one client-side presence timer.

**Fix shape:** the match id has to be one value, not two. Either lift it into `DuelRoot`
(`onMatchIdChange` from `useDuelLifecycle`, or move `activeMatch` up), or move the heartbeat and
`setLiveMatchId` down into `useDuelLifecycle` where `activeMatch.matchId` already lives — the
latter loses the "covers staging onward" property that put the beat in `DuelRoot` in the first
place, so lifting is probably right. A regression test is available cheaply and belongs in
`serverAuthoritativeWrites.test.ts` rather than in a component test: *create a match, finish it,
create its rematch, wait past the grace window without beating, assert the opponent forfeit is
refused* — which fails today for the right reason.

### 0.2 `round_start` is payload-trusted on the intermission fast path — HIGH — extends §3.4

§3.4 credits two handlers with re-verifying against authoritative state rather than trusting the
broadcast: *"`onForfeit` and `onRoundStart` **do** re-verify… `round_end` and `match_end` are the
two that don't."* The `onRoundStart` half of that credit is **only true in one of its two
branches** ([useDuelLifecycle.ts:176-208](../components/duel/useDuelLifecycle.ts#L176-L208)):

```ts
if (phase === "intermission" && intermission && !intermission.isLastRound
    && payload.roundIndex === intermission.nextRoundIndex) {
  adoptRound({ roundIndex: payload.roundIndex, startedAt: payload.startedAt, endsAt: payload.endsAt, ... });
  setIntermission(null);
  setPhase("playing");
  return;
}
void refreshRoundState();   // <- the branch that re-verifies
```

The fast path takes `startedAt` and `endsAt` **straight off the payload**. On an unauthenticated
channel (which `duel:{matchId}` still is — see §3.4 below, no `private: true`, no
`realtime.messages` policies anywhere in `drizzle/`) that hands an attacker the victim's round
clock:

- `endsAt` in the past → `useServerCountdown` reads 0 immediately, the round-expiry effect
  ([useDuelLifecycle.ts:608-614](../components/duel/useDuelLifecycle.ts#L608-L614)) fires
  `duel_close_round` at once, and the victim DNFs a round they never saw.
- `startedAt` far in the future → `isPreRound` stays true and the board never becomes live; the
  lights sequence sits there while the real server clock runs down.

Neither corrupts the database — `duel_submit_guess` validates the window server-side
([0022:199-208](../drizzle/0022_duel_submit_guess_rpc.sql#L199-L208)) — but both cost the victim the
round, which in a rated 1v1 is the point.

**This is a gap in the original finding, not a regression from §2.3's refactor.** I diffed the
handler against `git show 5b1a4aa:components/duel/DuelMatch.tsx` and it is character-identical; the
extraction moved it verbatim, as that resolution claims.

The real fix is §3.4's (`private: true` + `realtime.messages` RLS scoped to participants), which
closes this along with `round_end` and `match_end`. The cheap partial fix is one line: call
`refreshRoundState()` in the fast path too, and accept one extra round trip on a beat where the
client is already between rounds. Note the fast path exists for a reason — it is the "opponent's
ready-gate won the race" catch-up — so deleting it is not the answer.

### 0.3 The entire fix programme is uncommitted, so **CI has never run once** — HIGH (process)

`git status` against `main`:

```
HEAD = 5b1a4aa "UI upgrades"     <- the commit the FIRST audit was taken against
 M  49 files
 ?? .github/                     <- the CI workflow §2.6 exists to add
 ?? docs/                        <- both audit documents
 ?? .env.example                 <- the file .github/workflows/ci.yml tells you to read
 ?? drizzle/0038 … 0045          <- eight migrations, including all four security ones
 ?? components/duel/{DuelExitControl,duelMatchTypes,useDuelLifecycle,useDuelScoreboard,useRematchNegotiation}
 M  drizzle/meta/_journal.json
```

Every item on this list is real work that exists only in one working tree. Four consequences, and
the first is the one that matters:

1. **`.github/workflows/ci.yml` has never executed.** It is untracked, so no push has ever included
   it. Top-5 #5 — the item whose whole argument is *"a rule nothing runs is a rule nobody keeps"* —
   is at this moment a rule nothing runs. Its own resolution flagged the `build` job as unverified
   and said "**watch it on the first push**"; the first push hasn't happened. So the parity suites
   (`compare.sqlParity`, `duelScoring.sqlParity`), the grant policy (`schemaGrants`), the
   win-by-identity regression and the matchmaking integrity suites still run only when someone
   remembers — which is §2.6 restated, unchanged.
2. **The migration journal is modified but uncommitted.** The live database has 0038–0045 applied
   (per the previous audit's verification); a fresh clone does not have the files *or* the journal
   entries. Anyone else running `npm run db:migrate` today produces a schema that silently disagrees
   with production — no daily secret, no liveness columns, no revokes, tile-derived wins.
3. **Deleting is not the same as staging.** `PLAN-fast-guesses.md`, `scripts/_db-test-tmp.mjs` and
   `lib/game/dailySelection.sqlParity.test.ts` show as `D ` (staged) while
   `components/settings/SettingsSync.tsx` shows as ` D` (unstaged) — an inconsistent index state
   that is easy to half-commit.
4. **`scripts/_db-test-tmp.mjs`** was tracked and is staged for deletion; worth confirming nothing
   still references it before that lands.

This is not a code finding and it needs no design decision — but it gates the value of five other
findings' fixes, so it belongs at the top of the list. **Commit, push, and watch the three CI jobs.**

### 0.4 `migrateLocalStats` silently truncates the guess distribution — LOW

[actions.ts:98-100](../lib/stats/actions.ts#L98-L100):

```ts
const mergedDistribution = current.guessDistribution.map(
  (count, i) => count + (clean.guessDistribution[i] ?? 0),
);
```

`.map` over `current` preserves **current's** length. `sanitizeLocalStats` produces a
`MAX_GUESSES`-length (6) array, so if the server row holds a 5-bucket distribution — which is
exactly what any `user_stats` row created between drizzle/0007 and drizzle/0016 holds, see §0.6 —
the legacy player's 6-guess wins are dropped on the floor and the row stays 5 buckets forever.

Narrow (legacy accounts only, one bucket, cosmetic in the Statistics chart) and safe in the
direction that matters — it can only lose counts, never invent them. `Array.from({ length:
MAX_GUESSES }, (_, i) => (current.guessDistribution[i] ?? 0) + (clean.guessDistribution[i] ?? 0))`
fixes it and normalises the row's length at the same time.

### 0.5 The `exhaustive-deps` suppressions grew from 12 to 17 — LOW

§2.6 counted **12** `eslint-disable react-hooks/exhaustive-deps` comments, noted they suppress a
rule that never runs, and deliberately scoped the lint half out. There are now **17**, across 8
files — §2.3's extraction split `DuelMatch`'s eight across `useDuelLifecycle`, `useDuelChannel` and
the rest rather than resolving any of them, and `InfiniteGame`, `DuelIntermission`, `DuelSearching`,
`NewsCarousel` and `AuthProvider` carry the others.

Still exactly as decorative as before, and still not a reason to add ESLint inside another change.
It is a reason to note that the number moves in one direction on its own: the day the rule is
switched on, it will be 17+ pre-existing violations to triage, not 12. Recording it so the decision
is made against the real number.

### 0.6 Correction: §3.11's `guessDistribution` item was half-wrong when written

§3.11 says *"DB default is 5 ([0007:10](../drizzle/0007_daily_results_last_result.sql#L10)); the
Drizzle default is 6"* and concludes that trigger-created rows get 5 buckets. **That was already out
of date:** [0016_cheerful_wendigo.sql:1](../drizzle/0016_cheerful_wendigo.sql#L1) is
`ALTER TABLE "user_stats" ALTER COLUMN "guess_distribution" SET DEFAULT '[0,0,0,0,0,0]'::jsonb`. New
rows have had six buckets since 0016, and the "6-guess bar never renders" symptom does not affect
anyone who signed up after it.

What genuinely remains of the finding:

- **Rows created between 0007 and 0016 still hold 5 buckets.** Nothing backfilled them. They repair
  themselves on the first 6-guess win (`recordDailyResultForUser`'s
  `nextDistribution[index] = (nextDistribution[index] ?? 0) + 1` extends the array —
  [recordDailyResult.ts:91-93](../lib/stats/recordDailyResult.ts#L91-L93)), and never repair
  through `migrateLocalStats` (§0.4).
- **`StatisticsSection` still falls back to a 5-element array** —
  [StatisticsSection.tsx:25](../components/settings/StatisticsSection.tsx#L25),
  `stats?.guessDistribution ?? [0, 0, 0, 0, 0]` — so a viewer whose stats haven't loaded sees five
  bars, then six. `Array(MAX_GUESSES).fill(0)` is the one-line fix and removes the last hardcoded
  copy of the number outside SQL.

Downgraded from a four-way inconsistency to a two-way one. Still LOW.

---

## 1. Performance & Efficiency — what's open

**Closed and verified:** §1.0 (the duel render loop — `useServerCountdown` self-clears, `round` is
nulled on both terminal paths, five `memo()`s, `useServerDeadlinePassed`), §1.1's memo half,
§1.3 (the folded search index).

### 1.1 residual — the two-context split — MED

The memo landed exactly as prescribed ([AuthProvider.tsx:483-510](../components/auth/AuthProvider.tsx#L483-L510),
`signOutAndReset` is a `useCallback` at [:447](../components/auth/AuthProvider.tsx#L447)). The
**split** was explicitly scoped out and is still open: `stats` and `profile` sit in the same context
value as `userId`, so `refresh()` after a completed daily — which `DailyGame` calls on the finishing
guess ([DailyGame.tsx:273](../app/%28game%29/daily/DailyGame.tsx#L273)) — re-renders every
`useAuth()` consumer in the app, including the board that just finished. `identityStatus` / `status`
already mark where the seam goes. Ten call sites.

### 1.2 The daily countdown still re-renders the whole board every second — MED — **open**

[DailyGame.tsx:223-236](../app/%28game%29/daily/DailyGame.tsx#L223-L236) is unchanged.
`setCountdown(...)` at 1 Hz re-renders `DailyBoard` → `GuessGrid` (6 rows × 6 tiles), `ResultCard`,
`Modal`. Two things soften it since the last audit and neither removes it:

- `DriverAutocomplete` is now `memo()`'d and its props are stable here, so it is skipped.
- §4.4's fix means the 1 Hz tick no longer **steals focus** in the share modal — the symptom that
  made this urgent.

`GuessGrid` is still a plain function export ([GuessGrid.tsx:230](../components/game/GuessGrid.tsx#L230)),
so the full tile subtree is re-walked 60×/minute on the completed-board screen. Either fix works:
extract the countdown into a leaf that holds its own state (better — the ticking state then lives
where it renders), or `memo()` `GuessGrid`.

### 1.4 Bundle: all three payloads unchanged — MED — **open**

Re-checked, all three still exactly as described:

- [globals.css:2](../app/globals.css#L2) still `@import "flag-icons/css/flag-icons.min.css"` — the
  full ~250-country stylesheet on every route, against `lib/game/flags.ts`'s 40 mapped
  nationalities, for a setting that is **off by default** ([store.ts:17](../lib/settings/store.ts#L17)).
- **`next/dynamic` appears zero times in the repo.** `GameModals` statically imports both
  `LeaderboardModal` and `SettingsModal` ([GameModals.tsx:5-6](../components/layout/GameModals.tsx#L5-L6)),
  so DiceBear rides the critical path of `/daily` where no avatar is ever visible.
- `AuthProvider` is still in the root layout, so `RealtimeClient` is in the shared chunk on `/about`.

Unmeasured this pass — the numbers in §1.4 came from a `next build` that does not complete on this
machine (§2.6). Treat the shapes as verified and the KB figures as inherited.

### 1.5 Server Actions with serial round trips — MED — **open**

Unchanged in [lib/duel/actions.ts](../lib/duel/actions.ts):

| Function | Serial hops | Called from |
|---|---|---|
| `getDuelRoundState` [:44-84](../lib/duel/actions.ts#L44-L84) | `getUser` → match → round → myResult | the intermission poll, every 5 s ([useDuelLifecycle.ts:636](../components/duel/useDuelLifecycle.ts#L636)) |
| `getMyLiveMatch` [:302-343](../lib/duel/actions.ts#L302-L343) | `getUser` → match → `duel_state` → opponent stats | every `/online` mount, plus the staging backfill |
| `getDuelResults` [:376-422](../lib/duel/actions.ts#L376-L422) | `getUser` → match → rounds → results | the results panel |

The last two queries in each are independent and `Promise.all`-able. `getDuelRoundState` remains
the one poll in the app that hits a Server Action rather than a warm RPC.

### 1.6 `getLeaderboard` over-fetches and duplicates a query — MED — **open**

[lib/leaderboard/actions.ts](../lib/leaderboard/actions.ts), all three sub-items intact:

- `BOARD_SIZE = 50` [:9](../lib/leaderboard/actions.ts#L9) vs `TOP_SLOTS = 10`
  [LeaderboardModal.tsx:22](../components/leaderboard/LeaderboardModal.tsx#L22) — 100 rows fetched,
  20 rendered.
- `await getCurrentUserId()` [:52](../lib/leaderboard/actions.ts#L52) still runs **before** the two
  board queries it does not feed.
- `getMyDuelRank` and `getMyStreakRank` are now `Promise.all`'d, but each still fetches the
  **identical** `me` row ([:93](../lib/leaderboard/actions.ts#L93), [:116](../lib/leaderboard/actions.ts#L116))
  and then its own `count()` — 4 queries where a single window function would do 1.

### 1.7 Smaller items — LOW — **all three open**

- [Toast.tsx:38,45](../components/ui/Toast.tsx#L38-L45) — two `setTimeout`s, no stored handle, no
  cleanup. Cosmetic (React 19 no-ops the unmounted `setState`; the provider is app-root).
- [InfiniteGame.tsx:51-58](../app/%28game%29/infinite/InfiniteGame.tsx#L51-L58) — five
  792-element `.filter().length` calls where `.reduce()` is free. Correctly `useMemo`'d, so it runs
  once.
- [useLightsCountdown.ts:134,145](../components/duel/useLightsCountdown.ts#L134-L145) — `Date.now()`
  in the render body, non-idempotent under StrictMode double-render. Latent, not a bug.

---

## 2. Code Cleanliness — what's open

**Closed and verified:** §2.1 (dead exports gone; `queries.ts` is down to two roster reads;
`README.md` is real documentation now), §2.2, §2.3 (`DuelMatch.tsx` 1031 → **194** lines, the
machine in `useDuelLifecycle` + two composed hooks — read them, the split is clean), §2.4.

### 2.5 residual — the duplication itself, and the one cutoff with no guard at all — HIGH

The `duelScoring` parity suite exists now ([duelScoring.sqlParity.test.ts](../lib/game/duelScoring.sqlParity.test.ts)),
and pinning the **live** definition via `pg_get_functiondef()` rather than a transcription is the
right design. What that resolution lists as still outstanding is still outstanding, and one item on
the list is materially worse than the others:

- **`DAILY_POOL_WINDOW`'s ten-year cutoff is hardcoded in SQL in 9 migrations and has no test of any
  kind.** Grep for `- 10` across `drizzle/` returns 0013, 0017, 0019, 0020, 0021, 0028, 0030, 0033,
  0035, 0036, 0038. The two that are **live** are
  [0038:98](../drizzle/0038_daily_target_secret.sql#L98) (`daily_target_id`, which picks the day's
  answer) and [0036:64](../drizzle/0036_duel_countdown_no_slack.sql#L64) (`duel_begin_round`, which
  picks each duel round's target). Each carries a "keep in sync with
  `lib/game/poolWindow.ts#DAILY_POOL_WINDOW`" comment and nothing else.

  Change `DAILY_POOL_WINDOW` in TypeScript and only the **autocomplete** moves. The target keeps
  coming from the old window, so `/daily` can serve a driver that isn't in the list the player can
  type — an unguessable answer. That is precisely the bug class
  [dailySelection.ts:1-16](../lib/game/dailySelection.ts#L1-L16) documents having already been fixed
  once, for a different cause. It is also the only one of §2.5's duplications with **no guard**: the
  compare ladder has `compare.sqlParity`, the scoring weights have `duelScoring.sqlParity`, this has
  a comment. A single assertion in the DB tier — *the SQL cutoff year equals
  `poolCutoffYear(DAILY_POOL_WINDOW, currentYear)`* — would close it.

- **`MAX_GUESSES` = 6 is hardcoded in SQL** at [0044:179](../drizzle/0044_win_by_driver_identity.sql#L179)
  (`v_max constant integer := 6`) and [0044:311,326](../drizzle/0044_win_by_driver_identity.sql#L311)
  (`v_round.guess_count >= 6`), against `lib/game/constants.ts`. Lower consequence — a mismatch
  produces a board that ends at a different count, visibly — but same unguarded shape.

- The compare/scoring duplication itself is unchanged and, per that resolution, deliberately so.

### 2.6 residual — no lint, no component tests, and see §0.3 — MED

The workflow, `npm run typecheck`, and [schemaGrants.test.ts](../lib/db/schemaGrants.test.ts) are all
real and good. Three things remain:

- **CI has never run** — §0.3. This is the live half of §2.6, not a footnote.
- **The lint half is scoped out, and the number is now 17** — §0.5.
- **Still no component tests.** [vitest.config.mts](../vitest.config.mts) is `environment: "node"`
  with no jsdom or testing-library, so the hydration gates, the auth-reactive remounts and the duel
  phase machine have no coverage — and §0.1 above is exactly the class of bug a component test
  catches and a DB test doesn't. Three separate resolutions (§1.0, §4.5, §4.6) each closed with
  "no automated coverage added, for the same reason". That is four times the same gap has been the
  stated reason a fix went unverified; it is worth deciding on rather than re-noting.

---

## 3. Security & Data Integrity — what's open

**Closed and verified:** §3.0's XSS + open redirect, §3.1 (all three parts), §3.2, §3.3 (**but see
§0.1**), §3.7, §3.8, §3.10, §3.11's `user_stats` grant (via drizzle/0042).

### 3.4 Duel realtime channels are unauthenticated, and `round_end` is trusted — HIGH — **open, and now the top security item**

Verified unchanged. [useDuelChannel.ts:171-173](../lib/duel/useDuelChannel.ts#L171-L173):

```ts
const channel = supabase.channel(duelChannelName(matchId), {
  config: { presence: { key: safeMyUserId } },     // still no `private: true`
});
```

`grep -ril realtime drizzle/` returns only two files, and in both it is the word "realtime" inside a
comment — there are still **no `realtime.messages` RLS policies anywhere**. Supabase Realtime
Authorization is opt-in, so any signed-in user (and `AuthProvider` signs *everyone* in
automatically) can join `duel:{N}` for arbitrary `N` and post arbitrary events.

`onRoundEnd` still applies the payload directly
([useDuelLifecycle.ts:209-212](../components/duel/useDuelLifecycle.ts#L209-L212)) — a forged
`round_end` yanks the victim out of `playing` mid-round, shows an attacker-chosen driver reveal and
attacker-chosen scores, and parks them on an attacker-chosen `intermissionEndsAt`. **And
`onRoundStart` is now known to be trusted too, on its intermission branch — §0.2.**

Nothing here writes the database; `duel_submit_guess`, `duel_close_round` and `applyMatchRatings`
all validate independently. What it costs is the round, live, in a rated match. With §0.1 also open
this is the largest remaining hole in the duel: one bug lets you forfeit your rematch opponent
outright, the other lets you break their round in the match that preceded it.

Fix: `private: true` on the channel plus `realtime.messages` policies scoped to the match's two
participants. That closes §0.2 as a side effect, and is the only fix that closes `match_end` too.

### 3.5 The self-match guard's only server-side layer is liveness — MED — **open**

Unchanged. `device_id` is still `crypto.randomUUID()` in `localStorage`
([deviceId.ts:17-37](../lib/duel/deviceId.ts#L17-L37)) passed in by the client, so L1/L2/L4 are all
client-side and L3 (the 15 s window) is satisfied by simply heartbeating. The *design* reasoning
remains sound and no layer should be removed; the point stands that CLAUDE.md's "four independent
layers" reads stronger than the implementation is.

### 3.6 residual — display-name impersonation — LOW

Drizzle/0045 is a good fix and closed the part that mattered: the column grant (`is_guest` and
`username` are server-owned now) and the unbounded strings (`CHECK` constraints, added *validated*).
The finding's other clause — *"display-name impersonation"* — is untouched: `display_name` has no
uniqueness constraint and no check against existing `username`s, so two leaderboard rows can read
"Max Verstappen". Arguably correct product behaviour (display names usually aren't unique);
recording it so the omission is deliberate rather than assumed.

One cosmetic follow-on: a `CHECK` violation surfaces raw, as
`Something went wrong: <postgres message>` ([ProfileSection.tsx:88-90](../components/settings/ProfileSection.tsx#L88-L90)).
Unreachable through the UI (the input is `maxLength={32}` and the value is `.trim()`ed before send),
so it's only reachable by someone calling PostgREST directly — who deserves the raw message.

### 3.9 No duplicate-guess guard, no pool validation, no rate limiting — LOW/MED — **all three open**

Re-verified against the **live** RPC body (drizzle/0044, which supersedes 0031/0028):

- **No duplicate guard.** [0044:186-221](../drizzle/0044_win_by_driver_identity.sql#L186-L221)
  checks the driver exists and the day isn't complete, then `array_append`s unconditionally. Nothing
  rejects a driver already in `guesses[]`, and `DriverAutocomplete` doesn't filter guessed drivers
  out of its suggestions ([DriverAutocomplete.tsx:44](../components/game/DriverAutocomplete.tsx#L44)
  — `fuzzyFilter(query, searchIndex, 8)`, no exclusion set). Six guesses can be spent on one driver.
- **No pool validation.** `daily_submit_guess` accepts **any** `drivers.id`
  ([0044:186-189](../drizzle/0044_win_by_driver_identity.sql#L186-L189)), not just the 10-year pool
  the target is drawn from. Not exploitable (identity decides the win now, so an out-of-pool guess
  can never win) but it corrupts guess history and the shareable emoji grid.
  `infinite_start_round` validates its `p_pool_window` against an allowlist — the right pattern,
  still not applied here.
- **No rate limiting anywhere.** `infinite_start_round`'s unbounded `ORDER BY random()` full-pool
  scan and `match_or_queue`'s global per-pool advisory lock
  ([0032:181](../drizzle/0032_duel_matchmaking_integrity.sql#L181)) are both callable in a loop.

### 3.11 residual — three of five still open

- ✅ `guess_distribution` — **corrected and downgraded**, see §0.6.
- ✅ Teamless drivers comparing as an exact team match — fixed with drizzle/0044.
- ✅ The `user_stats` `UPDATE` grant — fixed, and far more thoroughly than the finding asked, by
  drizzle/0042.
- ❌ **`.env` still carries an unused `SESSION_SECRET`.** It is absent from `.env.example` (which is
  itself untracked, §0.3), so the only place it survives is the operator's local `.env` and any
  deployment env. Dead since infinite's round state moved to `infinite_rounds`. Hygiene.
- ❌ **The stale comment in 0021 is still there, still false, and it is the one that caused two
  findings.** [0021:13-17](../drizzle/0021_duel_lifecycle_rpcs.sql#L13-L17): *"Supabase revokes
  PUBLIC execute by default, so leaving that grant out keeps them unreachable from the
  anon/authenticated PostgREST roles entirely."* Directly contradicted by 0034, 0038 and 0039 —
  three migrations written to undo the consequences of believing it. Anyone reading `drizzle/` in
  order still meets this claim before they meet its refutation. CLAUDE.md states the rule correctly
  now and `schemaGrants.test.ts` enforces it, so the risk is reduced to "a reader is misled", but
  it costs one line to delete.
- ❌ **The `leaderboard` view's write grants.** Still recorded as `KNOWN OPEN` in
  [schemaGrants.test.ts:220-225](../lib/db/schemaGrants.test.ts#L220-L225): `DELETE, INSERT,
  REFERENCES, SELECT, TRIGGER, TRUNCATE, UPDATE` for both `anon` and `authenticated`. Inert only
  because a `JOIN` view is not auto-updatable, which is why the suite pins
  `is_updatable = 'NO'`. The one-line `REVOKE` migration §2.6 deferred is still deferred, and until
  it lands "don't flatten the leaderboard view" is a load-bearing security constraint held by a test
  comment.

### 3.1 residual — the explicit-`anon` revoke sweep — LOW

13 functions still carry bootstrap `anon`/`PUBLIC` grants, enumerated with a per-function reason at
[schemaGrants.test.ts:98-132](../lib/db/schemaGrants.test.ts#L98-L132) under a ratchet asserting the
count can only fall (`expect(open.length).toBeLessThanOrEqual(13)`). Hygiene, not exposure — each
reads `auth.uid()` as its first act, which is `NULL` for `anon`. The handling here is genuinely
good: recorded, not blessed, and a new function can't join the list without failing first.

### 3.0 residual — the site-wide CSP — MED

[next.config.ts](../next.config.ts) is still the empty `create-next-app` object. The per-response
nonce CSP covers `/auth/callback` alone, which is enough to close §3.0 (that is the app's only
server-rendered HTML built from request input), but the session cookies are still non-httpOnly by
`@supabase/ssr` design, so any *future* injection anywhere in the app is still a session takeover.
The scoping problem the resolution names is real — AdSense, Funding Choices, the Supabase WebSocket,
DiceBear, and Next's inline hydration bootstrap needing a nonce (which forces affected pages
dynamic). `Report-Only` first, as suggested.

---

## 4. Gameplay UX & Mechanics — what's open

**Closed and verified:** §4.1's tile labels (`tileLabel.ts` + `role="img"` — the right call),
§4.2, §4.3, §4.4 (fixed at `Modal`'s end, so it holds for `DuelMatch`'s exit modal too), §4.5's
`PoolSelect`, §4.6, §4.7's colorblind flash and `DriverCodeBadge`.

### 4.1 residual — nothing announces a submitted guess — MED (a11y)

The tiles are readable now; the **event** isn't. `grep` for `aria-live`/`role="status"` returns 12
hits, and every one is in the duel or a loading/toast surface. `GuessGrid` has none, and
`DailyGame`/`InfiniteGame` have none around it. A screen-reader user submits a guess and hears
nothing — they have to navigate back into the grid to find out what happened. One polite live region
saying the guessed driver and the summary verdict closes it; `guessTileLabels` already produces the
words.

### 4.5 residual — `InfoTopBar`'s dropdown makes the same ARIA promise — LOW

[InfoTopBar.tsx:86,110,117](../components/layout/InfoTopBar.tsx#L86) still sets
`role="combobox"` / `role="listbox"` / `role="option"` with **no `onKeyDown`, no `tabIndex`, no
`aria-activedescendant`** — verified, those three greps return nothing in that file. Not the same
severity as `PoolSelect` was: its options are `<Link>`s, so they are genuinely tabbable and the menu
is operable. But the roles now describe an interaction it doesn't implement, and it copies
`PoolSelect`'s markup — which now has the handlers — so the two have drifted apart in the direction
that reads as correct and isn't.

### 4.7 Smaller UX items — MED/LOW — **five of seven open**

- **Duplicate guesses burn a turn silently.** §3.9's client half — `DriverAutocomplete` never
  filters already-guessed drivers. Every comparable Wordle-style game blocks or warns.
- **Escape in the autocomplete doesn't `preventDefault`**
  ([DriverAutocomplete.tsx:77-79](../components/game/DriverAutocomplete.tsx#L77-L79) — `setIsOpen(false)`
  and nothing else), so inside a `Modal` it bubbles and closes the modal instead of just the
  dropdown. Also still missing `aria-autocomplete="list"`, Home/End, and ArrowUp-to-open (ArrowDown
  opens at [:59-62](../components/game/DriverAutocomplete.tsx#L59-L62); ArrowUp returns early and
  never opens).
- **Focus is lost when a duel round is solved.** `RoundPlay` swaps `DriverAutocomplete` for a static
  panel at [RoundPlay.tsx:145](../components/duel/RoundPlay.tsx#L145) with no `focus()` redirect —
  `grep focus() components/duel/RoundPlay.tsx` returns nothing. Focus falls to `<body>` and Tab
  restarts from the top, mid-match. Exactly the second-order problem §4.5's resolution found and
  fixed for `PoolSelect`; the same fix hasn't been applied one component over.
- **Tile clipping at 320 px with no recovery.** `grep title= components/game/GuessGrid.tsx
  components/duel/DuelIntermission.tsx` returns nothing, so "Scuderia AlphaTauri" still
  `line-clamp-2`s with no `title` attribute to recover the value. `showFlags` is off by default, so
  the default mobile nationality column is the clipped one. The `aria-label` from §4.1 means the
  value is now available *in speech* — which helps a screen-reader user and does nothing for a
  sighted user on a small phone.
- **The duel's autocomplete dropdown opens downward only** — `absolute z-10 mt-1 … max-h-64`
  ([DriverAutocomplete.tsx:123-128](../components/game/DriverAutocomplete.tsx#L123-L128)), no flip-up
  and no collision detection, in the one mode where guessing speed is scored.

---

## 5. Maintainability & F1 Data Management — what's open

**Closed and verified:** §5.1 (`f1db_id` + upsert + `rosterPlan.ts` + `--dry-run` + the
in-transaction invariants — this is the best-executed fix in the set), §5.2d's unique constraint.

### 5.2 The F1DB release is unpinned, and three failure modes are silent — MED — **open, and now the priority**

[seed.ts:18-19](../scripts/seed.ts#L18-L19) still fetches
`.../releases/latest/download/f1db-csv.zip`. There is no header assertion anywhere in the file — I
grepped; `positionText`, `positionNumber` and `round` are all read straight off the parsed row
([seed.ts:103-124](../scripts/seed.ts#L103-L124)) with no check that the column exists.

The three silent modes §5.2 tabulates are unchanged:

| Renamed upstream | Result |
|---|---|
| `positionText` | `NON_START_CODES.has(undefined)` → `false` → **every DNQ/DNS counts as a race start.** Debut years shift earlier, `last_active_year` later, pool membership changes. Silent. |
| `positionNumber` | `career_wins` becomes 0 for every driver. Silent. |
| `round` | `Number(undefined)` = `NaN`, tie-break never fires → wrong `last_team`. Silent. |

**§5.1's own resolution says this is now more urgent than when it was written, and it is right.**
The old seed's `DELETE FROM drivers` failed loudly against a foreign key; the new one **UPDATEs 792
live rows in place**. `MIN_ROSTER_RATIO = 0.9` ([seed.ts:35](../scripts/seed.ts#L35)) catches "most
of the feed is missing" and none of the three above — all three preserve the row count exactly. A
bad release now rewrites every driver's attributes, silently, inside a transaction that commits,
and the first symptom is players reporting that the comparisons are wrong.

Two ~15-line changes close it: a release pin in an env var with `latest` as an explicit opt-in, and
a header assertion plus two post-parse sanity checks (Hamilton ≥ 100 wins; Verstappen's
`last_active_year >= currentYear - 1`) **before** the transaction commits.

### 5.2b Silent slug fallbacks corrupt nationality and team comparison — MED — **open**

[seed.ts:213-222](../scripts/seed.ts#L213-L222), unchanged, and neither miss is counted or logged:

```ts
const nationality = countryNameById.get(row.nationalityCountryId) ?? row.nationalityCountryId;
const lastTeam = agg.lastConstructorId ? (constructorNameById.get(...) ?? agg.lastConstructorId) : null;
const previousTeams = [...agg.constructorIds].map((id) => constructorNameById.get(id) ?? id);
```

A miss stores `"united-states-of-america"`. `countryCode()` then returns `null` so the flag silently
vanishes, and because `compare_drivers` compares nationality by **string equality**, a
slug-nationality driver and a name-nationality driver *of the same country* report a nationality
**miss** against each other. Counting and logging the misses is the minimum; the seed already has a
`reportPlan` to print them into.

### 5.2c The nationality allow-list tests itself against a copy of itself — MED — **open**

[flags.test.ts:9](../lib/game/flags.test.ts#L9)'s `KNOWN_NATIONALITIES` is still a hand-copied
duplicate of [flags.ts](../lib/game/flags.ts)'s 40-entry map, with a comment claiming it was "pulled
live from the DB, not guessed" — which describes a one-time manual extraction, not what the test
does. It cannot detect a new nationality entering the roster.

Worth reweighting slightly upward now: the DB tier exists and runs on every push (once §0.3 lands),
so `SELECT DISTINCT nationality FROM drivers` → assert every one resolves is now a **cheap** test to
write and a place to put it. A new nationality degrades gracefully; a **renamed** one splits the
namespace and produces a nationality miss between two drivers of the same country — and §5.1 made
renames land silently by upsert rather than loudly.

### 5.2d residual — the value `check()` constraints — MED

[schema.ts:4-35](../lib/db/schema.ts#L4-L35) — `drivers` now has `.unique()` on `f1dbId` (0043) and
still has **no `check()` at all**. Nothing prevents `debut_year > last_active_year`,
`date_of_death < date_of_birth`, a negative `career_wins`, or a future `date_of_birth`.
`duel_matches` carries a `check()`, so the pattern is known. These are the per-column guard §5.2's
crude roster floor explicitly does not provide, on the table every mode reads from — and they are
the cheapest defence against a mis-parsed release, since they fail the seed's transaction.

### 5.1 residual — the real seed run — INFO

`npm run db:seed` (without `--dry-run`) has still not been run. Until it is, every row's `f1db_id`
is `NULL` — a valid and handled state, and the reason `f1db_id` is nullable — and the adoption
backfill plus refreshed wins/teams haven't landed. That is a production data refresh and the
operator's call, but note the ordering: **§5.2's pin and header assertions belong before the first
real run**, not after.

### 5.3 Data-quality edges — LOW/INFO — **two open**

- The RSS fetcher's `User-Agent` still advertises someone else's repository:
  `"DriverPitBot/1.0 (+https://github.com/f1db/f1db)"`
  ([fetchNews.ts:43](../lib/news/fetchNews.ts#L43)). Point it at your own site before traffic scales
  — five sources are being told to contact the F1DB maintainers about your crawler.
- `sort(() => Math.random() - 0.5)` at [seed.ts:376](../scripts/seed.ts#L376) is still a biased
  shuffle. It only picks a console preview sample. Cosmetic.
- The two `last_team` fallback inconsistencies §5.3 listed are **resolved** — the dead TS pair went
  with §2.1, leaving only the SQL `COALESCE(NULLIF(last_team, ''), '—')`.

---

## Top 5 — do these first

1. **Commit and push (§0.3).** Nothing else on this list is worth much until CI has run once. Eight
   migrations, the workflow, `.env.example`, five new duel modules and both audits exist in exactly
   one working tree, and the journal disagrees with the live database for everyone else. Watch all
   three jobs on the first push — the `build` job in particular has never executed anywhere, since
   `next build` doesn't complete on this machine.

2. **Restore the rematch heartbeat (§0.1).** §3.3's server-verified absence check is inert for every
   rematch, so `forfeitMatch(rematchId, opponentId)` is a guaranteed win with real Elo roughly ten
   seconds into round 1. One state value being owned in two places is the whole cause. Add the DB
   regression test alongside — it is a natural fit for `serverAuthoritativeWrites.test.ts` and fails
   today for the right reason.

3. **Turn on Realtime Authorization (§3.4 + §0.2).** `private: true` plus `realtime.messages`
   policies scoped to the match's two participants. It closes forged `round_end`, forged
   `match_end`, and the payload-trusted `round_start` fast path in one change — and it is the only
   fix that closes all three. Failing that, the one-line `refreshRoundState()` in the fast path is a
   real partial mitigation for the sharpest of them.

4. **Pin the F1DB release and assert its headers (§5.2), before the first real `db:seed`.** §5.1
   converted this from a loud failure into a silent one, on purpose and correctly — which means the
   loud check has to be rebuilt in the script. ~15 lines: an env-var pin, a header assertion, two
   post-parse sanity checks. Add §5.2d's `check()` constraints in the same pass; they are the
   per-column half of the same defence and they fail the transaction rather than the game.

5. **Pin the daily pool cutoff to SQL (§2.5).** The one duplicated rule with no guard at all, whose
   failure mode is an unguessable daily answer that nobody can report as a bug because the board
   looks fine. One assertion in the DB tier: the year `daily_target_id` filters on must equal
   `poolCutoffYear(DAILY_POOL_WINDOW, currentYear)`. Do `MAX_GUESSES` in the same test while you're
   there.

**Runners-up, in rough order:** the `leaderboard` `REVOKE` (§3.11) and the 0021 comment deletion
(§3.11), both one-liners; a live region for submitted guesses (§4.1) and focus-on-solve in
`RoundPlay` (§4.7), both a11y and both small; the duplicate-guess guard on both sides (§3.9 + §4.7);
`Promise.all` in the three duel Server Actions and `getLeaderboard` (§1.5, §1.6); `memo()`/extract
the daily countdown (§1.2); `next/dynamic` on the two global modals (§1.4); the seed's slug-fallback
counters (§5.2b) and the live-DB nationality test (§5.2c). Then decide deliberately about the two
standing gaps rather than re-noting them: **component tests** (§2.6 — four resolutions have now
closed with "not verified in a browser") and **ESLint** (§0.5 — 17 suppressions and rising).

---

## Verification notes

- `npx tsc --noEmit` — **clean**.
- `npx vitest run` — **167 passed, 113 skipped, 26 files** (14 passed, 12 skipped). The skipped
  files are the `RUN_DB_INTEGRATION_TESTS=1` tier: the two parity suites, the grant policy, the
  win-by-identity regression, the matchmaking and RPC suites, and the roster integrity suite.
- Migration journal vs. directory — **46 entries, 46 files**, no orphan in either direction; last
  tag `0045_profiles_column_grants`.
- **Not run:** the DB integration tier (no scratch database reachable here, and CLAUDE.md records
  that a full run exhausts Supabase's per-IP anonymous sign-in quota for ~an hour) and `next build`
  (stalls before compiling on this machine — CLAUDE.md and §2.6). Every statement above about the
  live database's grants, constraints or RPC bodies is therefore **inherited from the previous
  audit's verification**, not re-measured. Where I make a claim about SQL, it is a claim about the
  migration text in `drizzle/`.
- §0.1 and §0.2 are **reasoned from the source**, not reproduced in a browser: §0.1 from the state
  ownership in `DuelRoot` / `useDuelLifecycle` plus `duel_heartbeat`'s `status NOT IN ('finished',
  'abandoned')` guard, §0.2 from the handler body. Both are cheap to confirm — §0.1 by starting a
  rematch and watching for `duel_heartbeat` in the network tab (there won't be one), §0.2 by
  sending a `round_start` on `duel:{id}` from a second signed-in session during an intermission.
- §0.2 was additionally checked against `git show 5b1a4aa:components/duel/DuelMatch.tsx` to confirm
  it is a gap in the original finding rather than a regression introduced by §2.3's refactor. It is
  character-identical.
